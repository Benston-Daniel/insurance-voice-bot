version: '3.8'
services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./frontend:/app/frontend:ro
    environment:
      - NLU_ENGINE=sentence-transformers  # or llama-cpp / transformers
      - LLM_MODEL_NAME=mistral-7b  # configure as needed
